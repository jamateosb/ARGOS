# ARGOS runtime dependencies (the single source of dependency information).
# Lower bounds are the versions the code requires; the exact versions used for
# the published evaluation are listed in docs/reproducibility.md.

# Orchestrator and node APIs
fastapi>=0.110
uvicorn>=0.23
httpx>=0.27
pydantic>=2.0
psutil>=5.9
PyYAML>=6.0
nats-py>=2.8          # optional NATS control-plane events (imported only when enabled)

# Learned controllers (DQN, PPO). A CPU-only build is sufficient.
torch>=2.0

# Analysis and figures
numpy>=1.24
pandas>=2.0
scipy>=1.10
matplotlib>=3.7
